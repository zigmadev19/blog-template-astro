---
interface Props {
	date: Date;
}

const { date, class: className, ...rest } = Astro.props;
---

<time datetime={date.toISOString()} class={className} {...rest}>
	{
		date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
